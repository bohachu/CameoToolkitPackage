using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestDialogue : MonoBehaviour
{
    public DialogueSet test;
    // Start is called before the first frame update
    void Start()
    {
        test.StartDialogues();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
